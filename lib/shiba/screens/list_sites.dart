import 'package:flutter/material.dart';

import 'body_list_screen.dart';

class ListSites extends StatelessWidget {
  const ListSites({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: BodyListScreen(),
    );
  }
}
